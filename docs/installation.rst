============
Installation
============

At the command line::

    $ easy_install django-requirements-tool

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-requirements-tool
    $ pip install django-requirements-tool
